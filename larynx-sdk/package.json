{
  "name": "larynx-sdk",
  "version": "2.0.0",
  "scripts": {
    "test": "tslint -c tslint.json 'tests/**/*.ts' && mocha --opts tests/mocha.opts tests/**/*-test.ts",
    "build": "rm -rf lib && tslint -c tslint.json 'src/**/*.ts' &&  tsc -p . && rm -fr lib/tests && mv lib/src/** lib/ && rm -fr lib/src",
    "prepublish": "npm run build"
  },
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "SEE LICENSE IN License.txt",
  "files": [
    "lib/"
  ],
  "devDependencies": {
    "@types/chai": "^3.4.35",
    "@types/mocha": "^2.2.40",
    "@types/node": "^7.0.11",
    "chai": "^3.5.0",
    "mocha": "^3.1.0",
    "mocha-clean": "^1.0.0",
    "ts-node": "^1.3.0",
    "tslint": "^4.5.1",
    "typescript": "^2.0.3"
  },
  "dependencies": {
    "@types/chai": "^3.4.35",
    "@types/mocha": "^2.2.40",
    "typings": "^2.1.0"
  }
}
